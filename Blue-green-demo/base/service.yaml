apiVersion: v1
kind: Service
metadata:
  name: demo-service
spec:
  selector:
    app: demo
    version: blue   # Start with blue as active
  ports:
  - protocol: TCP
    port: 80
    targetPort: 5678
  type: NodePort

